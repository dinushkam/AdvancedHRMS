<UserControl x:Class="AdvancedHRMS.Views.AttendanceWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Attendance Controls -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button x:Name="BtnCheckIn" Content="Check In" Click="CheckIn_Click" 
                    Width="120" Height="40" Margin="5,0" FontSize="14"/>
            <Button x:Name="BtnCheckOut" Content="Check Out" Click="CheckOut_Click" 
                    Width="120" Height="40" Margin="5,0" FontSize="14" IsEnabled="False"/>
        </StackPanel>

        <!-- Attendance History -->
        <DataGrid x:Name="AttendanceGrid" Grid.Row="1" Margin="10" AutoGenerateColumns="False"
                 IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat={}{0:d}}" Width="120"/>
                <DataGridTextColumn Header="Check In" Binding="{Binding CheckInTime, StringFormat={}{0:t}}"/>
                <DataGridTextColumn Header="Check Out" Binding="{Binding CheckOutTime, StringFormat={}{0:t}}"/>
                <DataGridTextColumn Header="Hours Worked" Binding="{Binding HoursWorked}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Summary -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Today: " FontWeight="Bold" Margin="5"/>
            <TextBlock x:Name="TxtTodayHours" Text="0 hours" Margin="5"/>
            <TextBlock Text="This Month: " FontWeight="Bold" Margin="5"/>
            <TextBlock x:Name="TxtMonthHours" Text="0 hours" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>